# Copy to .env and fill in your own values.

# Ticketmaster Discovery API key (free at developer.ticketmaster.com)
TICKETMASTER_API_KEY=

# External SQL Server / Azure SQL (used by the API for logging).
# Leave empty if you do not want DB logging.
DB_SERVER=
DB_PORT=1433
DB_NAME=
DB_USER=
DB_PASSWORD=
